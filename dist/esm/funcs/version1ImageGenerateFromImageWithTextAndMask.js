/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { appendForm, encodeJSON, encodeSimple } from "../lib/encodings.js";
import { readableStreamToArrayBuffer } from "../lib/files.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import * as errors from "../models/errors/index.js";
import * as operations from "../models/operations/index.js";
import { APIPromise } from "../types/async.js";
import { isBlobLike } from "../types/blobs.js";
import { isReadableStream } from "../types/streams.js";
export var GenerateFromImageWithTextAndMaskAcceptEnum;
(function (GenerateFromImageWithTextAndMaskAcceptEnum) {
    GenerateFromImageWithTextAndMaskAcceptEnum["applicationJson"] = "application/json";
    GenerateFromImageWithTextAndMaskAcceptEnum["imagePng"] = "image/png";
})(GenerateFromImageWithTextAndMaskAcceptEnum || (GenerateFromImageWithTextAndMaskAcceptEnum = {}));
/**
 * Image-to-image with a mask
 *
 * @remarks
 * Selectively modify portions of an image using a mask. The `mask` must be the same shape and size as the init image. This endpoint also supports `image` parameters with alpha channels.  See below for more details.
 *
 * > NOTE: Only **Version 1** engines will work with this endpoint.
 */
export function version1ImageGenerateFromImageWithTextAndMask(client, request, options) {
    return new APIPromise($do(client, request, options));
}
async function $do(client, request, options) {
    const parsed = safeParse(request, (value) => operations.GenerateFromImageWithTextAndMaskRequest$outboundSchema.parse(value), "Input validation failed");
    if (!parsed.ok) {
        return [parsed, { status: "invalid" }];
    }
    const payload = parsed.value;
    const body = new FormData();
    if (isBlobLike(payload.MaskingRequestBody.init_image)) {
        appendForm(body, "init_image", payload.MaskingRequestBody.init_image);
    }
    else if (isReadableStream(payload.MaskingRequestBody.init_image.content)) {
        const buffer = await readableStreamToArrayBuffer(payload.MaskingRequestBody.init_image.content);
        const blob = new Blob([buffer], { type: "application/octet-stream" });
        appendForm(body, "init_image", blob);
    }
    else {
        appendForm(body, "init_image", new Blob([payload.MaskingRequestBody.init_image.content], {
            type: "application/octet-stream",
        }), payload.MaskingRequestBody.init_image.fileName);
    }
    appendForm(body, "mask_source", payload.MaskingRequestBody.mask_source);
    appendForm(body, "text_prompts", encodeJSON("text_prompts", payload.MaskingRequestBody.text_prompts, {
        explode: true,
    }));
    if (payload.MaskingRequestBody.cfg_scale !== undefined) {
        appendForm(body, "cfg_scale", payload.MaskingRequestBody.cfg_scale);
    }
    if (payload.MaskingRequestBody.clip_guidance_preset !== undefined) {
        appendForm(body, "clip_guidance_preset", payload.MaskingRequestBody.clip_guidance_preset);
    }
    if (payload.MaskingRequestBody.extras !== undefined) {
        appendForm(body, "extras", encodeJSON("extras", payload.MaskingRequestBody.extras, {
            explode: true,
        }));
    }
    if (payload.MaskingRequestBody.mask_image !== undefined) {
        if (isBlobLike(payload.MaskingRequestBody.mask_image)) {
            appendForm(body, "mask_image", payload.MaskingRequestBody.mask_image);
        }
        else if (isReadableStream(payload.MaskingRequestBody.mask_image.content)) {
            const buffer = await readableStreamToArrayBuffer(payload.MaskingRequestBody.mask_image.content);
            const blob = new Blob([buffer], { type: "application/octet-stream" });
            appendForm(body, "mask_image", blob);
        }
        else {
            appendForm(body, "mask_image", new Blob([payload.MaskingRequestBody.mask_image.content], {
                type: "application/octet-stream",
            }), payload.MaskingRequestBody.mask_image.fileName);
        }
    }
    if (payload.MaskingRequestBody.sampler !== undefined) {
        appendForm(body, "sampler", payload.MaskingRequestBody.sampler);
    }
    if (payload.MaskingRequestBody.samples !== undefined) {
        appendForm(body, "samples", payload.MaskingRequestBody.samples);
    }
    if (payload.MaskingRequestBody.seed !== undefined) {
        appendForm(body, "seed", payload.MaskingRequestBody.seed);
    }
    if (payload.MaskingRequestBody.steps !== undefined) {
        appendForm(body, "steps", payload.MaskingRequestBody.steps);
    }
    if (payload.MaskingRequestBody.style_preset !== undefined) {
        appendForm(body, "style_preset", payload.MaskingRequestBody.style_preset);
    }
    const pathParams = {
        engine_id: encodeSimple("engine_id", payload.engine_id, {
            explode: false,
            charEncoding: "percent",
        }),
    };
    const path = pathToFunc("/v1/generation/{engine_id}/image-to-image/masking")(pathParams);
    const headers = new Headers(compactMap({
        Accept: options?.acceptHeaderOverride
            || "application/json;q=1, image/png;q=0",
        "Organization": encodeSimple("Organization", payload.Organization, {
            explode: false,
            charEncoding: "none",
        }),
        "Stability-Client-ID": encodeSimple("Stability-Client-ID", payload["Stability-Client-ID"], { explode: false, charEncoding: "none" }),
        "Stability-Client-Version": encodeSimple("Stability-Client-Version", payload["Stability-Client-Version"], { explode: false, charEncoding: "none" }),
    }));
    const secConfig = await extractSecurity(client._options.stabilityApiKey);
    const securityInput = secConfig == null ? {} : { stabilityApiKey: secConfig };
    const requestSecurity = resolveGlobalSecurity(securityInput);
    const context = {
        baseURL: options?.serverURL ?? client._baseURL ?? "",
        operationID: "generateFromImageWithTextAndMask",
        oAuth2Scopes: [],
        resolvedSecurity: requestSecurity,
        securitySource: client._options.stabilityApiKey,
        retryConfig: options?.retries
            || client._options.retryConfig
            || { strategy: "none" },
        retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
    };
    const requestRes = client._createRequest(context, {
        security: requestSecurity,
        method: "POST",
        baseURL: options?.serverURL,
        path: path,
        headers: headers,
        body: body,
        timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
    }, options);
    if (!requestRes.ok) {
        return [requestRes, { status: "invalid" }];
    }
    const req = requestRes.value;
    const doResult = await client._do(req, {
        context,
        errorCodes: ["400", "401", "403", "404", "4XX", "500", "5XX"],
        retryConfig: context.retryConfig,
        retryCodes: context.retryCodes,
    });
    if (!doResult.ok) {
        return [doResult, { status: "request-error", request: req }];
    }
    const response = doResult.value;
    const responseFields = {
        HttpMeta: { Response: response, Request: req },
    };
    const [result] = await M.match(M.json(200, operations.GenerateFromImageWithTextAndMaskResponse$inboundSchema, { hdrs: true, key: "Result" }), M.stream(200, operations.GenerateFromImageWithTextAndMaskResponse$inboundSchema, { ctype: "image/png", hdrs: true, key: "Result" }), M.jsonErr([400, 401, 403, 404], errors.ErrorT$inboundSchema), M.jsonErr(500, errors.ErrorT$inboundSchema), M.fail("4XX"), M.fail("5XX"))(response, { extraFields: responseFields });
    if (!result.ok) {
        return [result, { status: "complete", request: req, response }];
    }
    return [result, { status: "complete", request: req, response }];
}
//# sourceMappingURL=version1ImageGenerateFromImageWithTextAndMask.js.map